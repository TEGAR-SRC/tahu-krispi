# Kilat Cloud Documentation — Environment configuration
# Copy to .env and fill in values. NEVER commit real secrets.

# Application
VITE_APP_TITLE=Kilat Cloud Docs

# API base URL. In dev the Vite dev server proxies /api -> http://localhost:8080,
# so this is only used outside Vite dev (e.g. a built preview or separate host).
VITE_API_BASE_URL=https://api-docs.kilat-cloud.com

# Local dev port
VITE_PORT=5176
