// MongoDB service detail — full parity skeleton lives in k4-dbEngine.tsx.
import { DatabaseServicePage } from "./k4-dbEngine"

export default function DokployMongoServicePage() {
  return <DatabaseServicePage kind="mongo" />
}
