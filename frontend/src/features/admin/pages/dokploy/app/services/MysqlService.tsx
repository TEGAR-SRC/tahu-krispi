// MySQL service detail — full parity skeleton lives in k4-dbEngine.tsx.
import { DatabaseServicePage } from "./k4-dbEngine"

export default function DokployMysqlServicePage() {
  return <DatabaseServicePage kind="mysql" />
}
